---
title: Blogroll
description: Blogroll
---
<h1 class="posts">Blogroll</h1><br>
<ul class="grid roll" role="list">
    {% for blog in blogData %}
    
        <li class="card">
            <h2>
                <img alt="none" src="{{ blog.favicon }}" width="32px" height="32px" loading="lazy" decoding="async" /> 
                <a style="text-decoration: none" href="{{ blog.url | url }}" target="_blank" class="blogroll">
                    {{ blog.name }}
                </a>
            </h2>
            {{ blog.cleansedUrl }}<br>
            {% if blog.latestPost and blog.latestPost.title %}
            <span style="color: var(--color-head); font-size: var(--font-size-l)">
                <b>Latest post:</b></span>
                <a href="{{ blog.latestPost.url | url }}" target="_blank" class="blogpost">
                    {{ blog.latestPost.title }}
                </a>
            {% endif %} 
        </li>
    
    {% endfor %}
</ul>

<style>
  .roll {
    margin-bottom: var(--space-3);
  }
  .blogroll a {
    text-decoration: none;
  }
  
  .blogpost a {
    text-decoration-color: var(--color-link);
    text-decoration-thickness: .200em;
    text-underline-offset: 4px;
    color: var(--color-text);
  }
  
  .blogroll:hover {
   outline: 1px solid var(--color-link-hover);
   background-color: var(--color-link-hover);
   color: var(--color-background);
   text-decoration: none;
  }
  
   .blogpost:hover {
   outline: 1px solid var(--color-link-hover);
   background-color: var(--color-link-hover);
   color: var(--color-background);
   text-decoration: none;
  }
</style>