<style>
  .card {
    background: var(--color-background);
    color: var(--color-text);
    text-wrap: balance;
    margin: var(--space-2);
    padding: var(--space-3);
    max-inline-size: auto;
    box-shadow: 0px 3px 6px 0px var(--color-box);
  }
</style>