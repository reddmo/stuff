<style>
  .card {
    background: var(--color-card);
    color: var(--color-text);
    text-wrap: balance;
    margin: var(--space-2);
    padding: var(--space-3);
    max-inline-size: auto;
    border: 3px solid var(--color-link-hover);
    border-radius: 6px;
  }
  
  .new {
    background: var(--color-card);
    color: var(--color-text);
    text-wrap: balance;
    margin: var(--space-2);
    padding: var(--space-3);
    max-inline-size: auto;
    border: 3px dotted var(--color-link-hover);
    border-radius: 6px;
  }
</style>