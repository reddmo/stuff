<style>
  .card {
    background: var(--color-background);
    color: var(--color-text);
    text-wrap: balance;
    margin: var(--space-2);
    padding: var(--space-3);
    max-inline-size: auto;
    box-shadow: 0px 4px 11px 0px var(--slate-12);
  }
</style>