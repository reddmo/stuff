<style>
  .card {
    background: var(--color-card);
    color: var(--color-text);
    text-wrap: balance;
    margin: var(--space-2);
    padding: var(--space-3);
    max-inline-size: auto;
    border: 3px solid var(--color-text);
    border-radius: 6px;
  }
</style>